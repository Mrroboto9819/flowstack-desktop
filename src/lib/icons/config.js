// Which icon set the whole app renders.
//   "phosphor" - Phosphor Icons (MIT), ~9000 icons, 6 weights
//   "lucide"   - Lucide (MIT), the original set
// Flip this one value to switch everything back. Both packages stay installed,
// so the change is a single edit with no import churn.
export const ICON_SET = "phosphor";
